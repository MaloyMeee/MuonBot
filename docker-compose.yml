version: '3'
services:
  muon-bot:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    environment:
      - TOKEN=${TOKEN}
    volumes:
      - ./bot:/app/bot
    command: python bot/main.py
